{
  "name": "epluris-dashboard",
  "private": true,
  "workspaces": ["epluris"],
  "devDependencies": {
    "tailwindcss": "^4",
    "autoprefixer": "^10"
  },
  "scripts": {
    "build": "npm --prefix epluris run build",
    "build:epluris": "npm --prefix epluris run build"
  }
}
